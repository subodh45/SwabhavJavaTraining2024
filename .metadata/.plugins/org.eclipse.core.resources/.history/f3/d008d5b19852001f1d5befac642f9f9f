package com.aurionpro.game.model;

import static org.junit.jupiter.api.Assertions.*;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

class BoardTest {

	Board game;
	@BeforeEach
	void setUp() throws Exception {
		Marker board[][] = { { Marker._, Marker._, Marker._ }, { Marker._, Marker._, Marker._ },
				{ Marker._, Marker._, Marker._ } };
		game = new Board(board);
	}

	
	void testDisplay() {
	
	}

	@Test
	void testCheckWin() {
		assertTrue(game.board[0][0] == Marker.O && game.board[0][1] == Marker.O && game.board[0][2] == Marker.O, "got it");
	}

}
